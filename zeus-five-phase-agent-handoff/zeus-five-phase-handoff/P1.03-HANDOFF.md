# P1.03 handoff

**Ticket:** P1.03 — separate identities and fail-closed authorization  
**Findings:** V01–V05 and L11 addressed in code with focused tests on this runner; **not** certified closed (G1 / qualified review still required). V07 remains contained, not closed.  
**Depends on:** P1.01, P1.02  
**Date:** 2026-09-08

## Starting / ending commit

Workspace `c:\Users\markmv\Desktop\Zeus` is not a git checkout in this environment. Planning baseline cited by the pack: `02ef77cdc3b1c1cd27ae618eeeff93c761c062a9`. **Implementation commit: NOT RECORDED.** Git commit/push/pull/fetch were skipped by request.

## Changed files

- `server/auth.go` — `Principal`, `credentialPermits`, `authPrincipal`
- `server/server.go` — bound ingest `ServerID`
- `server/enroll.go` — handshake-only enroll scope; read cannot DELETE; ambiguous names
- `server/session.go` — enroll/kind checks on login
- `server/auth_test.go`, `server/p103_identity_test.go` (new)
- `storage/apikeys.go` — exact hashing, no default all/admin, no un-revoke, `LookupAPIKey`
- `storage/opaque_tenant.go` (new) — SQLite backup + atomic remap
- `storage/apikeys_test.go`, `storage/p103_identity_test.go` (new)
- `main.go` — no CSV auto-import; insert-if-absent seed; `-migrate-opaque-tenants`
- `p103_seed_test.go` (new)
- `CHANGELOG.md`, `USER_GUIDE.md`, `ADMIN_GUIDE.md`

## Migration / backward compatibility

- `UpsertAPIKey` requires explicit `Kind` and `Scope`; conflict updates no longer set `revoked_at = 0`.
- `HashAPIKey` hashes trimmed exact bytes. Existing tokens stored under the old WM-normalization of `wma_`/`wmr_`/`wmk_` may need re-issue. Enrollment codes use `HashEnrollCode`.
- Default SQL column defaults remain `legacy`/`all` for **old rows**; those credentials cannot pass platform-admin checks.
- Opaque tenant remap is SQLite-only, transactional, backup-first. Rollback = restore the backup file with the Hub stopped. Never downgrade into legacy plaintext tenant IDs or CSV auto-import.
- PostgreSQL opaque remap: **not implemented** (CLI exits). Snapshot PG separately.

## Commands and real results

Runner: Windows 10/amd64. `go version` = `go1.26.5 windows/amd64`. Isolation: `WMONITOR_TEST_ISOLATION=1`.

```
go test -count=1 -timeout 180s ./storage ./server .
ok  	Zeus/storage	1.874s
ok  	Zeus/server	2.136s
ok  	Zeus	0.458s
exit 0
```

`go vet ./...`: **NOT RUN** (focused packages compiled and tested). Native Linux: **NOT RUN**. PostgreSQL identity tests: **NOT RUN**. Browser: **NOT RUN**. `go test -race ./...`: **NOT RUN**.

Acceptance covered by tests:

- Route × method × kind × scope matrix (`TestP103RouteMethodKindScopeMatrix`)
- Token for A cannot write B (`TestP103AgentTokenCannotWriteSiblingServer`)
- Revoked keys stay revoked after reopen + simulated upsert (`TestP103RevokedKeyStaysRevokedAfterRestart`, `TestUpsertDoesNotUnrevoke`, `TestP103AutoSeedDoesNotImportCSVOrUnrevoke`)
- Duplicate display names never resolve (`TestP103DuplicateClientNamesNeverResolve`)
- Migration fails atomically and does not un-revoke (`TestMigrateOpaqueTenantsAtomicAndKeepsRevoked`)
- Read cannot revoke (`TestP103ReadTokenCannotRevokeAgent`)

## Unresolved blockers

- No git; cannot attach an implementation commit.
- Auth cache TTL / replica invalidation remains P1.04 (V18/V19).
- Tenant scope in storage/alerts/exports remains P1.05 (V06).
- HTTPS/proxy/file ACLs remain P1.06.
- Sessions/CSRF remain P1.07.
- PostgreSQL opaque-tenant migration not shipped.
- Exact hashing may break previously stored case-normalized token hashes until rotation.
- Phase 1 and G1 are not done. V07 is not closed. Do not re-enable `downsampleMetrics`.

## Rollback

Revert the listed files. If `-migrate-opaque-tenants` ran, restore the SQLite backup; do not reverse the remap with a code downgrade that re-enables CSV auto-import or `revoked_at = 0` upserts.

## Next ticket

**P1.04** — atomic enrollment, rotation, and auth-cache lifetime. Do not start P1.05–P1.10. Keep `WMONITOR_TEST_ISOLATION=1`. Do not claim Phase 1 complete.
