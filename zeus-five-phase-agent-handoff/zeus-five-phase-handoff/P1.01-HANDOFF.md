# P1.01 handoff

**Ticket:** P1.01 — isolate tests and establish a truthful baseline  
**Finding:** V22 (resolved by tests on this runner); V21/L17 begun, not closed  
**Date:** 2026-09-07

## Starting / ending commit

Workspace `c:\Users\markmv\Desktop\Zeus` is not a git checkout in this environment (`git` is not installed). Planning baseline cited by the pack: `02ef77cdc3b1c1cd27ae618eeeff93c761c062a9`. **Implementation commit: NOT RECORDED.**

## Changed files

- `internal/fsroot/fsroot.go`, `internal/fsroot/fsroot_test.go` (new)
- `internal/testisolate/isolate.go` (new)
- `agent/credstore.go`, `agent/credstore_unix.go`, `agent/credstore_windows.go`, `agent/credstore_test.go`
- `agent/agent.go`, `agent/agent_test.go`, `agent/spool.go`, `agent/spool_test.go`, `agent/isolation_test.go`
- `storage/db.go`, `storage/datadir_test.go`
- `.github/workflows/ci.yml`, `.github/workflows/release.yml`
- `go.mod`, `go.sum`
- `CHANGELOG.md`, `USER_GUIDE.md`, `ADMIN_GUIDE.md`

## Migration / backward compatibility

No schema change. Production token/data **paths** are unchanged. Resolving those paths no longer creates directories unless credentials are saved. New env vars `WMONITOR_CREDENTIAL_DIR`, `WMONITOR_DATA_DIR`, `WMONITOR_TEST_ISOLATION` are additive. Rollback: revert the listed files; no DB repair.

## Commands and real results

Runner: Windows 10/amd64. `go version` initially `go1.26.5 windows/amd64`; `GOTOOLCHAIN=go1.26.6` downloaded and used for the patched scan.

```
go test -count=1 ./internal/fsroot ./internal/testisolate ./agent ./storage
ok   Zeus/internal/fsroot  0.480s
?    Zeus/internal/testisolate  [no test files]
ok   Zeus/agent            2.668s
ok   Zeus/storage          2.536s
exit 0
```

V22 canary (also PASS):

```
$env:WMONITOR_TEST_ISOLATION="1"
go test -count=1 -v ./agent -run "TestCredentialFixtureDoesNotTouchProductionCanary|TestSetCredentialDirRejectsProduction|TestDefaultCredentialDir|TestCredentialsSaveLoadClear|TestNewSpoolRejectsProductionPath"
--- PASS: TestNewSpoolRejectsProductionPath
--- PASS: TestCredentialsSaveLoadClear
--- PASS: TestSetCredentialDirRejectsProduction
--- PASS: TestDefaultCredentialDirIsPlatformIsolatedFromFixtures
--- PASS: TestCredentialFixtureDoesNotTouchProductionCanary
ok  Zeus/agent  1.129s
```

Linux `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go test -c ./agent` compiled (exit 0). Native Linux execution of the Unix credential path was **NOT RUN**.

```
go mod verify     # exit 0 (after tidy)
go list -m golang.org/x/text  →  golang.org/x/text v0.39.0
go vet ./...      # exit 0
```

govulncheck `v1.1.4`, DB https://vuln.go.dev updated **2026-09-02 19:12:04 UTC**:

| Toolchain | Result |
|---|---|
| go1.26.5 module+binary | 6 **stdlib** findings, all fixed in go1.26.6 (includes GO-2026-5026). GO-2026-5970 (`x/text` < v0.39.0) **not reported** after the upgrade. Two additional unused import/require hits were stdlib go1.26.5 issues, not `x/text`. |
| go1.26.6 `govulncheck ./...` | `No vulnerabilities found.` exit 0 |

staticcheck `@v0.6.1` on `./internal/fsroot ./agent ./storage` with go1.26.6: exit 0, no findings printed.

`go test -race ./...` (full tree, including 35s collector test): **NOT RUN** in this ticket after isolation landed; CI is the intended broad runner and now inherits `WMONITOR_TEST_ISOLATION=1`. PostgreSQL CRUD: **NOT RUN**. Browser tests: **NOT RUN**.

## Fixture / result hashes

Canary bytes are generated per run (`v22-canary-<unixnano>`). The acceptance check is SHA-256 equality before/after the suite (`fsroot.VerifyUnchanged` + external canary), not a stable corpus hash. Production sensitive files (token.json/dat, config.env, agent_id, sqlite DBs under well-known roots) were snapshotted and verified unchanged by `TestMain` / `TestCredentialFixtureDoesNotTouchProductionCanary`.

## Unresolved blockers

- No git; cannot attach an implementation commit.
- Native Linux/root `/etc/wmonitor` behavior not executed; Unix path logic is compile-checked only.
- V21 not closed: CI must actually run on GitHub with 1.26.6; selected-module/binary gates remain P1.10/P5.07. Local default toolchain is still 1.26.5 unless `GOTOOLCHAIN=go1.26.6`.
- L17 not closed: no real PostgreSQL/browser/service fixtures; Actions still use version tags (`v4.2.2` / `v5.5.0`), not commit SHAs; scanners are pinned but not signature-verified.
- Full `go test ./...` on this enrolled-capable Windows host was not re-run as a broad suite after isolation (focused packages were).

## Rollback

Revert the changed files. No identity/schema migration. Do not downgrade into a tree that still writes production tokens from tests.

## Next ticket

**P1.02** — containment controls and safe export rendering (`retention/retention.go`, `export/report.go`, `export/export.go`). Unsafe retention stays disabled until P2.03. Do not start P1.03 until P1.02 acceptance exists.
