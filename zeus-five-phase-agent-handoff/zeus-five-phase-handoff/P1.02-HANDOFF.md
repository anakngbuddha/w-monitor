# P1.02 handoff

**Ticket:** P1.02 — containment controls and safe export rendering  
**Findings:** V07 contained (not closed); V08/V09 addressed in exporters; L18 CHANGELOG claim corrected; V13 operator guidance only (no automatic secret rotation)  
**Depends on:** P1.01  
**Date:** 2026-09-07

## Starting / ending commit

Not a git workspace in this environment. Implementation commit: **NOT RECORDED**.

## Changed files

- `retention/retention.go`, `retention/retention_test.go`
- `export/report.go`, `export/export.go`, `export/export_test.go`
- `server/server.go`, `server/server_test.go`
- `main.go`
- `CHANGELOG.md`, `USER_GUIDE.md`, `ADMIN_GUIDE.md`

## Migration / backward compatibility

No schema change. Hourly downsampling and 30-day purge no longer run. Disk may grow until `WMONITOR_DISK_BUDGET_BYTES`. HTML report markup changed (template + CSP meta, no emoji headings). `-export-csv` now prefixes formula-like Server ID/hostname. Rollback: revert files; do not restore `downsampleMetrics` onto a database that already mixed tenants.

## Commands and real results

```
go test -count=1 ./retention ./export ./server
ok  Zeus/retention  0.532s
ok  Zeus/export     1.398s
ok  Zeus/server     3.112s
exit 0
```

Spreadsheet application (Excel/Sheets) execution of formula fixtures: **NOT RUN** (string-prefix tests only). Native Linux: **NOT RUN**. PostgreSQL purge path: **NOT RUN** (same containment: `Run` does not call `Pruner.PurgeOld`).

## Unresolved blockers

- V07 remains open until P2.03 typed rollups with ownership.
- Health `retention` object is visible; operators must still watch disk.
- No automatic credential rotation (explicitly out of scope).
- P1.03–P1.10 and G1 are not done.

## Rollback

Revert the listed files. Unsafe downsampling functions remain in `retention.go` but are not called; deleting that dead code is optional and not required to stay contained.

## Next ticket

**P1.03** — separate identities and fail-closed authorization.
