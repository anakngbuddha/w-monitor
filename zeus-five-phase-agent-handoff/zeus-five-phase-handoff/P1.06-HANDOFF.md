# P1.06 handoff

**Ticket:** P1.06 — transport, destination, and secret-file boundaries  
**Findings:** V10/V11/V13/V14/V20 addressed in focused tests on this runner; **not** certified closed. Native second-user ACL, live PostgreSQL, and browser checks are **NOT RUN**. V07 remains contained.  
**Depends on:** P1.01–P1.05  
**Date:** 2026-09-08

## Starting / ending commit

Workspace `c:\Users\markmv\Desktop\Zeus` is not a git checkout. **Implementation commit: NOT RECORDED.**

## Changed files

- `agent/enroll.go` (RequireHTTPSHub)
- `agent/transport.go`, `agent/transport_test.go`
- `agent/spool.go`, `agent/spool_test.go`
- `agent/agent.go`
- `agent/credstore.go`, `agent/credstore_unix.go`, `agent/credstore_windows.go`
- `agent/p106_secretfile_test.go`, `agent/p106_dacl_windows_test.go` (new)
- `server/https.go`, `server/https_test.go` (existing from this ticket’s earlier work)
- `main.go`, `p106_config_test.go` (new)
- `install.ps1`
- `CHANGELOG.md`, `USER_GUIDE.md`, `ADMIN_GUIDE.md`

## Migration / backward compatibility

- Production agents refuse non-HTTPS hub URLs except loopback.
- Stored credentials bound to a different hub origin are not sent. Re-enroll at the new hub.
- Spool segments for a previous origin/tenant/server are moved to `spool/quarantine/` and are not drained.
- Remote Postgres without `sslmode=require|verify-ca|verify-full` fails at startup.
- Windows `install.ps1` writes `%ProgramData%\wmonitor\config.env` (not the installing user’s LocalAppData). Service arguments no longer include `-api-key`.
- `-print-config` prints `(set)` / `(not set)` for secrets; it no longer shows a prefix/suffix of the value.
- Do not re-enable `downsampleMetrics`. Existing `wma_`/`wmr_`/`wmk_` hashes from the P1.03 hashing change may still need re-issue.

## Commands and real results

```
$env:WMONITOR_TEST_ISOLATION='1'; go test -count=1 -timeout 180s ./agent ./server .
ok  	Zeus/agent	2.154s
ok  	Zeus/server	7.775s
ok  	Zeus	0.586s
exit 0
```

Windows DACL contents (SYSTEM + Administrators, no Everyone/Users ACE) asserted after `SaveCredentials`. A second non-admin Windows user read/replace attempt: **NOT RUN**. PostgreSQL live TLS: **NOT RUN**. Native Linux ownership/symlink: **NOT RUN** on this Windows runner (Unix code paths compiled via build tags only). Browser: **NOT RUN**. `go test -race ./...`: **NOT RUN**.

`retention.Job.Run()` still does not call `downsampleMetrics`.

## Unresolved blockers

- P1.07–P1.10 and G1 remain.
- G1 native OS service/ACL with a standard user, live PG, and browser evidence still required before Phase 1 exit.
- V07 not closed.

## Rollback

Revert listed files. Agents that already wrote `spool/bind.json` keep working with the new code; removing bind support would send quarantined backlog again if those files are moved back.

## Next ticket

**P1.07** — actual secure sessions and browser boundaries. Do not start P1.08–P1.10. Keep `WMONITOR_TEST_ISOLATION=1`. Do not claim Phase 1 complete.
