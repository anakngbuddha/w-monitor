# P1.04 handoff

**Ticket:** P1.04 — atomic enrollment, rotation, and auth-cache lifetime  
**Findings:** V18/V19 addressed in code with focused tests on this runner; **not** certified closed (G1 / qualified review still required). V07 remains contained, not closed.  
**Depends on:** P1.01, P1.02, P1.03  
**Date:** 2026-09-08

## Starting / ending commit

Workspace `c:\Users\markmv\Desktop\Zeus` is not a git checkout in this environment. Planning baseline cited by the pack: `02ef77cdc3b1c1cd27ae618eeeff93c761c062a9`. **Implementation commit: NOT RECORDED.** Git commit/push/pull/fetch were skipped by request.

## Changed files

- `storage/enrollment.go` (new) — `CompleteEnrollment`, `ReplaceAgent`, handshake + `auth_epoch`, one-active-agent unique index
- `storage/enrollment_pg.go` (new) — Postgres counterparts
- `storage/enrollment_test.go` (new)
- `storage/apikeys.go` — revoke bumps epoch; `TouchAPIKey` drops handshake plaintext; ResolveAPIKey expiry uses `>=`
- `server/enroll.go` — transactional enroll; `current_token`; `POST /api/admin/agents/rotate`; DELETE invalidates hashes
- `server/auth.go` — cache TTL min(60s, expiry); epoch-aware cache; `AuthRevocationMaxDelay` = 5s
- `server/server.go` — rotate route; epoch state
- `server/auth_cache_test.go` (new)
- `server/p104_enrollment_test.go` (new)
- `server/enroll_test.go` — lost-reply vs proof-required rotation
- `agent/enroll.go` — `current_token`
- `main.go` — `Enroll` extra argument (empty current token on first run)
- `CHANGELOG.md`, `USER_GUIDE.md`, `ADMIN_GUIDE.md`

## Migration / backward compatibility

- `POST /api/enroll` for an already-enrolled `server_id` no longer silently rotates. Clients must send `current_token` or an admin must call `/api/admin/agents/rotate`.
- Lost replies within `HandshakeTTL` (5 minutes), before first ingest, still return the same token.
- Schema additive: `enrollment_handshakes`, `auth_epoch`, unique partial index `idx_api_keys_one_active_agent`.
- Existing case-normalized `wma_`/`wmr_`/`wmk_` hashes from before P1.03 may still need re-issue.
- Do not re-enable `downsampleMetrics`.

## Commands and real results

Runner: Windows 10/amd64. Isolation: `WMONITOR_TEST_ISOLATION=1`.

```
go test -count=1 -timeout 180s ./storage ./server
ok  	Zeus/storage	5.293s
ok  	Zeus/server	9.768s
exit 0
```

`go vet ./...`: **NOT RUN** in this ticket after the focused packages passed. Native Linux: **NOT RUN**. PostgreSQL enrollment: **NOT RUN**. Browser: **NOT RUN**. `go test -race ./...`: **NOT RUN**.

Acceptance covered by tests:

- Concurrent same-identity enrollments (`TestCompleteEnrollmentConcurrentSameIdentity`, `TestP104ConcurrentEnrollOneActiveToken`)
- Failed insert does not consume a use (`TestCompleteEnrollmentFailedInsertDoesNotConsume`)
- Lost reply recovers the same token (`TestCompleteEnrollmentLostReplyRecoversSameToken`, `TestEnrollIdempotentRotation`)
- Replacement without proof is 409; with `current_token` or operator rotate succeeds
- Warmed cache + replica epoch (`TestP104WarmedCacheReplicaRevoke`, `TestAuthCacheMissesOnNewerEpoch`)
- Cache cannot outlive credential expiry (`TestAuthCacheBoundedByCredentialExpiry`, `TestP104ExpiredCredentialNotCachedPastExpiry`)

## Unresolved blockers

- No git; cannot attach an implementation commit.
- Tenant scope in storage/alerts/exports remains P1.05 (V06).
- HTTPS/proxy/file ACLs remain P1.06.
- Sessions/CSRF remain P1.07.
- PostgreSQL enrollment/rotate live tests: **NOT RUN**.
- Phase 1 and G1 are not done. V07 is not closed. Do not re-enable `downsampleMetrics`.

## Rollback

Revert the listed files. Handshake/epoch tables are unused after revert. Do not restore enroll handlers that rotate on code possession alone.

## Next ticket

**P1.05** — tenant scope across storage, compute, and reports. Do not start P1.06–P1.10. Keep `WMONITOR_TEST_ISOLATION=1`. Do not claim Phase 1 complete.
