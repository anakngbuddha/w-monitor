# P1.05 handoff

**Ticket:** P1.05 — tenant scope across storage, compute, and reports  
**Findings:** V06/V15 and L09/L10/L13 addressed in SQLite-focused tests on this runner; **not** certified closed. PostgreSQL RLS **NOT RUN**. V07 remains contained.  
**Depends on:** P1.01–P1.04  
**Date:** 2026-09-08

## Starting / ending commit

Workspace `c:\Users\markmv\Desktop\Zeus` is not a git checkout. **Implementation commit: NOT RECORDED.**

## Changed files

- `storage/query.go` (new)
- `storage/db.go`, `storage/postgres.go`, `storage/store.go`
- `storage/p105_scope_test.go` (new), `storage/db_test.go`
- `server/auth.go`, `server/server.go`, `server/alerts.go`
- `alerting/evaluator.go`, `alerting/alert.go`, `alerting/evaluator_test.go`
- `export/export.go`, `export/report.go`, `export/export_test.go`
- `collector/collector.go`, `collector/collector_test.go`
- `main.go`
- `CHANGELOG.md`, `USER_GUIDE.md`

## Migration / backward compatibility

- `QueryMetrics`/`QueryProcesses`/`QueryServers` reject empty tenant (`ErrTenantRequired`).
- `CSVReport`/`TextReport`/`GenerateAssessmentReport` take a tenant argument.
- CLI `-tenant` defaults to `t_local` when omitted (not all tenants).
- New inserts with empty tenant_id are stored as `t_local`. Existing empty rows are not rewritten.
- Do not re-enable `downsampleMetrics`.

## Commands and real results

```
go test -count=1 -timeout 180s ./storage ./server ./export ./alerting
ok  	Zeus/storage	4.632s
ok  	Zeus/server	8.288s
ok  	Zeus/export	1.751s
ok  	Zeus/alerting	0.381s
exit 0
```

PostgreSQL RLS / live PG isolation: **NOT RUN**. Native Linux: **NOT RUN**. Browser: **NOT RUN**. `go test -race ./...`: **NOT RUN**.

## Unresolved blockers

- P1.06–P1.10 and G1 remain.
- PostgreSQL least-privilege/RLS not tested (plan: introduce where tested, not merely configured).
- V07 not closed.

## Rollback

Revert listed files. CLI that still calls `CSVReport` with three arguments will not compile against this tree.

## Next ticket

**P1.06** — transport, destination, and secret-file boundaries. Do not start P1.07–P1.10. Keep `WMONITOR_TEST_ISOLATION=1`. Do not claim Phase 1 complete.
